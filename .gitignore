__pycache__/
*.pyc
*.log
*.tmp
.claude/
.DS_Store
.venv/
venv/
out/
*.egg-info/
build/
# local secrets / env overrides — NEVER commit (.env.example is the tracked template)
.env
# runtime artifacts when running bare (out_dir="."): status/state/events/csv land in cwd
status_*.json
*_state.json
*_events.jsonl
*_adv.csv
.coverage
# Bitget backtest data (BG-prefixed) — research-only, fed to BACKTEST only. Live reads
# NONE of data/ (market data = adapter WS feed + _rest_kline REST bootstrap). Zero live impact.
data/BG*.csv

# 买单 24h 时间钉死改单 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 把买单从"每 tick 追 bid 下移"改成"锚定 anchor + 挂上后 24h 钉死不改",逼价格来撞、拿成交。

**Architecture:** 买价 `floor(min(anchor−1bp, ask−1tick))`(anchor 主导天然稳、ask 兜顶防 taker);改单受 per-slice `last_place_ts` + `rebuy_min_hold_sec` 冷却;去买侧 3bp band;新增买侧下限 `rebuy_floor_px`。所有新参数默认值 = 关闭(now=None/min_hold=0/floor=0/ask=None),保证回测与旧测试逐字节不变。

**Tech Stack:** Python 3.11, pytest。运行:`PYTHONPATH=src python -m pytest tests/ -q`

**Spec:** `docs/superpowers/specs/2026-07-01-rebuy-24h-hold-design.md`

---

## File Structure

| 文件 | 职责 | 本次改动 |
|---|---|---|
| `src/sca/strategy_rules.py` | 纯定价函数 | `rebuy_price_raw`/`rounded_rebuy_price`:`bid`→`ask` cap 语义 |
| `src/sca/live/order_recon.py` | 纯 desired/diff 逻辑 | `desired_orders` 加 `ask`/`now`/`min_hold_sec`/`rebuy_floor_px`:冷却 + 下限 |
| `src/sca/live/engine.py` | live 引擎/状态 | 加载新 config;传 `ask`/`now`;`last_place_ts` 字段;`_place` 记录;去 buy band;`_status_rebuy_price` 传 ask |
| `src/sca/config.py` | config defaults | `_STRATEGY_PARAM_DEFAULTS` 加 `rebuy_min_hold_sec`/`rebuy_floor_px` |
| `config/strategy.yaml` | 运行配置 | 加两个 key |
| `tests/test_strategy_rules.py` | 定价单测 | 改 rebuy 测试为 ask 语义 |
| `tests/test_order_recon.py` | recon 单测 | 加冷却/下限/去 band 测试 |

---

## Task 1: `rebuy_price_raw` 换 anchor 主导 + ask 兜顶

**Files:**
- Modify: `src/sca/strategy_rules.py:129-146`
- Modify: `src/sca/live/order_recon.py:110-131`(desired_orders 签名 + buy 分支)
- Modify: `src/sca/live/engine.py:868`(`_status_rebuy_price`), `:763`, `:1752-1757`(reconcile 调用)
- Test: `tests/test_strategy_rules.py:27-32`, `tests/test_order_recon.py:118-125`

- [ ] **Step 1: 改 strategy_rules 测试为 ask 语义**

替换 `tests/test_strategy_rules.py` 中现有的两个 rebuy 测试(约 27-32 行)为:

```python
def test_rebuy_is_anchor_minus_offset_when_ask_high():
    # ask far above -> anchor-led (anchor - 1bp), ask cap inactive
    assert rebuy_price_raw(1.0009, -1, ask=1.0020) == pytest.approx(1.0008)


def test_rebuy_capped_at_ask_minus_tick_when_ask_low():
    # ask - 1tick sits below anchor-1bp -> cap wins
    assert rebuy_price_raw(1.0011, -1, ask=1.0010, tick=1e-4) == pytest.approx(1.0009)


def test_rebuy_no_ask_falls_back_to_anchor_offset():
    # backtest path: no live book -> pure anchor - 1bp (unchanged)
    assert rebuy_price_raw(1.0009, -1) == pytest.approx(1.0008)
```

- [ ] **Step 2: 跑测试验证失败**

Run: `PYTHONPATH=src python -m pytest tests/test_strategy_rules.py -q`
Expected: FAIL(`rebuy_price_raw() got an unexpected keyword argument 'ask'` 或断言不符)

- [ ] **Step 3: 改 `rebuy_price_raw` + `rounded_rebuy_price`**

`src/sca/strategy_rules.py` 行 129-146 替换为:

```python
def rebuy_price_raw(anchor: float, rebuy_off_bp: float,
                    ask: float | None = None, tick: float = BP) -> float:
    """Raw rebuy limit before tick quantization.

    Anchor-led: rests at ``anchor + off*BP`` (off negative => below anchor), which
    is stable because the anchor is a 1h EMA. Capped at ``ask - tick`` so the passive
    buy never crosses up into the ask (no taker / PostOnly reject). ``ask is None``
    (backtest has no live book) => pure ``anchor + off*BP`` — backtest unchanged.
    """
    base = float(anchor) + float(rebuy_off_bp) * BP
    a = _finite(ask)
    if a is not None:
        base = min(base, a - float(tick))
    return base


def rounded_rebuy_price(anchor: float, rebuy_off_bp: float,
                        ndigits: int = 4, ask: float | None = None,
                        tick: float = BP) -> float:
    return round(rebuy_price_raw(anchor, rebuy_off_bp, ask, tick), ndigits)
```

- [ ] **Step 4: 跑测试验证通过**

Run: `PYTHONPATH=src python -m pytest tests/test_strategy_rules.py -q`
Expected: PASS

- [ ] **Step 5: 改调用点传 ask(order_recon + engine)**

`src/sca/live/order_recon.py` `desired_orders` 签名(约 110-115 行)把 `bid` 改为 `ask`:

```python
def desired_orders(anchor, slices, rungs, rebuy_off_bp, tick, lot,
                   avail_base, avail_quote, min_qty, min_cost,
                   min_profit_bp=0.0, rest_bps=0.0,
                   ask: float | None = None,
                   sell_round: str = "ceil",
                   min_sell_margin_bp: float = 0.0) -> dict[int, Desired]:
```

买单分支(约 130 行)`raw = rebuy_price_raw(anchor, rebuy_off_bp, bid)` 改为:

```python
            raw = rebuy_price_raw(anchor, rebuy_off_bp, ask, tick)
```

`src/sca/live/engine.py`:
- 行 868 `_status_rebuy_price`:`rebuy_price_raw(anchor, self.rebuy_off_bp, self.bid)` → `rebuy_price_raw(anchor, self.rebuy_off_bp, self.ask, tick)`
- 行 869 `rounded_rebuy_price(anchor, self.rebuy_off_bp, TICK_DP)` 保持(paper 分支无 ask,fallback anchor−1bp)
- 行 763(`evaluate_fills` 模拟撮合)**保持不改**:它用 `B` 判断 `ask <= B` 触发模拟成交,若 `B` 改用 ask 算则 `B=ask−1tick`、`ask<=B` 永假 → 模拟买入永不成交(Codex #6)。这是 paper/backtest 模拟路径,与 live 挂单定价无关,保留 `self.bid` 语义
- **grep 所有 keyword 调用**:`grep -rn "rebuy_price_raw(\|rounded_rebuy_price(" src/ tests/`,把任何 `bid=` 关键字调用(如 `tests/test_bitget_pnl_fee_agnostic.py:55`)改为 `ask=` 或删关键字用位置参(Codex #7,否则 TypeError)
- reconcile(约 1755 行)`desired_orders(... bid=self.bid, ...)` → `ask=self.ask`

- [ ] **Step 6: 改 order_recon 买单测试(desired 用 ask,None 时不变)**

`tests/test_order_recon.py` `test_desired_usdt_is_buy_at_rebuy_qty_cash_over_price`(118-125)不传 ask → `ask=None` fallback,断言 `0.9999` 不变,应仍 PASS。追加一条 ask cap 测试:

```python
def test_desired_buy_capped_by_ask():
    slices = [_slice("usdt", cash=8.0)]
    out = desired_orders(1.0011, slices, rungs=[5], rebuy_off_bp=-1, tick=TICK, lot=LOT,
                         avail_base=0.0, avail_quote=8.0, min_qty=LOT, min_cost=1.0,
                         ask=1.0010)
    assert out[0].price == pytest.approx(1.0009)   # floor(min(1.0010, 1.0009))
```

- [ ] **Step 7: 全量测试验证绿**

Run: `PYTHONPATH=src python -m pytest tests/ -q`
Expected: PASS(回测/dashboard 相关测试因 ask=None fallback 不变)

- [ ] **Step 8: Commit**

```bash
git add src/sca/strategy_rules.py src/sca/live/order_recon.py src/sca/live/engine.py tests/test_strategy_rules.py tests/test_order_recon.py
git commit -m "refactor(live): rebuy 定价改 anchor 主导 + ask 兜顶(去 bid 追随)"
```

---

## Task 2: 新增配置 `rebuy_min_hold_sec` + `rebuy_floor_px`(默认关,USDC opt-in)

> **Codex #1 修正**:两个新特性默认 **OFF**(`_STRATEGY_PARAM_DEFAULTS` 里 0/0.0),只在 **USDC 的 universe override** 里开,保证 USD1(无 override)行为逐字节不变。绝不能把 86400/0.9990 写进全局 defaults——那会让正常成交的 USD1 也被 24h 冷却波及。

**Files:**
- Modify: `src/sca/config.py:71-86`(`_STRATEGY_PARAM_DEFAULTS`)
- Modify: `config/strategy.yaml`(USDCUSDT universe override 下加 `strategy:` 子块)
- Modify: `src/sca/live/engine.py:340-350`(加载 self.*)
- Test: `tests/test_config.py`(若无则新建)

- [ ] **Step 1: 写 config 测试(默认关 + USDC 开)**

在 `tests/test_config.py`(参考现有 strategy_for 测试;若文件不存在则新建并 `from sca.config import strategy_for`)追加:

```python
def test_rebuy_hold_floor_default_off():
    # no-override symbol (USD1) -> features OFF, zero behaviour change
    sp = strategy_for("USD1USDT")
    assert sp["rebuy_min_hold_sec"] == 0
    assert sp["rebuy_floor_px"] == 0.0


def test_rebuy_hold_floor_on_for_usdc():
    # USDC (厚盘口零成交 target) opts in via its universe override
    sp = strategy_for("USDCUSDT")
    assert sp["rebuy_min_hold_sec"] == 86400
    assert sp["rebuy_floor_px"] == 0.9990
```

- [ ] **Step 2: 跑测试验证失败**

Run: `PYTHONPATH=src python -m pytest tests/test_config.py -k rebuy_hold_floor -q`
Expected: FAIL(KeyError:'rebuy_min_hold_sec')

- [ ] **Step 3: 加 config defaults(=关)**

`src/sca/config.py` `_STRATEGY_PARAM_DEFAULTS`(71-86)在 `min_sell_margin_bp` 后加:

```python
    "rebuy_min_hold_sec": 0,      # 买单钉死时长(秒);0=关(默认→USD1 零变化)。厚盘口 symbol 在 universe override 开 86400
    "rebuy_floor_px": 0.0,        # 买侧下限;0=关(默认)。厚盘口 symbol override 开 0.9990
```

- [ ] **Step 4: 跑测试(默认关已过 / USDC 仍缺 override)**

Run: `PYTHONPATH=src python -m pytest tests/test_config.py -k rebuy_hold_floor -q`
Expected: `test_rebuy_hold_floor_default_off` PASS;`test_rebuy_hold_floor_on_for_usdc` 仍 FAIL(尚无 yaml override)

- [ ] **Step 5: 加 USDC universe override(只 USDC 生效)**

`config/strategy.yaml` 在 **USDCUSDT 的 universe 条目**下加 `strategy:` 子块(现有 apr/exchange 保持):

```yaml
  - symbol: USDCUSDT
    apr: 0.0
    exchange: bitget
    strategy:
      rebuy_min_hold_sec: 86400  # 24h 钉死不改价,逼价格来撞拿成交(仅 USDC 厚盘口)
      rebuy_floor_px: 0.9990     # 买侧下限,脱锚急跌停止挂买
```

- [ ] **Step 6: 跑测试验证两条都过**

Run: `PYTHONPATH=src python -m pytest tests/test_config.py -k rebuy_hold_floor -q`
Expected: PASS(both)

- [ ] **Step 7: engine 加载 self.***

`src/sca/live/engine.py` 约 348 行(`self.reprice_tol_bp = ...` 后)加:

```python
        self.rebuy_min_hold_sec = float(_sp["rebuy_min_hold_sec"])
        self.rebuy_floor_px = float(_sp["rebuy_floor_px"])
```

- [ ] **Step 8: 全量测试 + commit**

Run: `PYTHONPATH=src python -m pytest tests/ -q` → PASS(USD1/回测走默认关,零变化)

```bash
git add src/sca/config.py config/strategy.yaml src/sca/live/engine.py tests/test_config.py
git commit -m "feat(config): rebuy_min_hold_sec/rebuy_floor_px 默认关,USDC override 开(USD1 零变化)"
```

---

## Task 3: 新增 `last_place_ts` 字段 + `_place` 记录

**Files:**
- Modify: `src/sca/live/engine.py:172-176`(`_ORDER_FIELD_DEFAULTS`), `:1410-1420`(`_clear_slice_order`), `:1790-1824`(`_place`), `:1788`(reconcile 调 _place)
- Modify: `tests/test_order_recon.py:37-42`(`_slice` helper 加 last_place_ts)
- Test: `tests/test_engine_last_place_ts.py`(新建;参考现有 `tests/test_engine*.py` 的 fake-client 构造)

- [ ] **Step 1: 写字段默认值测试**

新建 `tests/test_engine_last_place_ts.py`:

```python
from sca.live.engine import _ORDER_FIELD_DEFAULTS


def test_last_place_ts_in_order_field_defaults():
    assert "last_place_ts" in _ORDER_FIELD_DEFAULTS
    assert _ORDER_FIELD_DEFAULTS["last_place_ts"] is None
```

- [ ] **Step 2: 跑测试验证失败**

Run: `PYTHONPATH=src python -m pytest tests/test_engine_last_place_ts.py -q`
Expected: FAIL(KeyError / assert)

- [ ] **Step 3: 加字段默认值 + _place 记录 + _clear 清除 + 传 now**

`src/sca/live/engine.py`:

`_ORDER_FIELD_DEFAULTS`(172-176)加 `"last_place_ts": None,`:

```python
_ORDER_FIELD_DEFAULTS = {
    "order_id": None, "order_link_id": None, "order_px": None, "order_side": None,
    "order_qty": None, "filled_qty": 0.0, "order_gen": 0, "reject_streak": 0,
    "sell_proceeds": 0.0, "qty_sold": 0.0, "last_place_ts": None,
}
```

`_place` 签名(1790)加 `now`:`def _place(self, action, client, now):`;**在 pre-call intent 段**(`s["order_px"] = ...` 那几行、`self._persist_durable_or_halt()` 之前)加 `s["last_place_ts"] = now`,与 `order_px` 一起落盘(Codex #3:place 成功但 final persist 前 crash → resume 时 `last_place_ts=None` 会立即改价、丢 hold;跟 intent 一起持久化则 resume 保住)。reject/too_small/insufficient 分支已走 `_clear_slice_order` 清除,无需额外处理。

并在 `_migrate_order_fields`(engine ~611-640)的字段类型校验表加一条 `"last_place_ts": lambda x: x is None or isinstance(x, (int, float))`(Codex #4:防损坏快照的非数字值让 `now - last_place_ts` 崩 reconcile)。

**先 grep 所有 `_place(` 调用点**:`grep -n "self\._place(" src/sca/live/engine.py`,把**每一处** `self._place(a, client)` 改为 `self._place(a, client, now)`(reconcile:1788 是主路径;若 seed/resume/其他路径也调 `_place`,同样传 `now`,否则漏掉处 TypeError)。

`_clear_slice_order`(1410-1420)在函数末尾加 `s["last_place_ts"] = None`(清除 → 重挂视为首次、立即)。

- [ ] **Step 4: 跑测试验证通过**

Run: `PYTHONPATH=src python -m pytest tests/test_engine_last_place_ts.py -q`
Expected: PASS

- [ ] **Step 5: 更新 order_recon `_slice` helper(为后续冷却测试备好字段)**

`tests/test_order_recon.py:37-42` `_slice` 返回 dict 末尾加 `"last_place_ts": None`:

```python
def _slice(state, qty=0.0, cash=0.0, order_id=None, order_link_id=None,
           order_px=None, order_side=None, order_qty=None, order_gen=0,
           last_place_ts=None):
    return {"state": state, "qty": qty, "cash": cash, "sell_px": 0.0, "entry": None,
            "order_id": order_id, "order_link_id": order_link_id, "order_px": order_px,
            "order_side": order_side, "order_qty": order_qty, "order_gen": order_gen,
            "filled_qty": 0.0, "last_place_ts": last_place_ts}
```

- [ ] **Step 6: 全量测试 + commit**

Run: `PYTHONPATH=src python -m pytest tests/ -q` → PASS

```bash
git add src/sca/live/engine.py tests/test_engine_last_place_ts.py tests/test_order_recon.py
git commit -m "feat(live): slice 加 last_place_ts(挂单时刻),_place 记录/_clear 清除"
```

---

## Task 4: 买侧下限 `rebuy_floor_px`(算出价过低不挂)

**Files:**
- Modify: `src/sca/live/order_recon.py`(`desired_orders` 加 `rebuy_floor_px` 参数 + skip)
- Modify: `src/sca/live/engine.py`(reconcile 传 `rebuy_floor_px=self.rebuy_floor_px`)
- Test: `tests/test_order_recon.py`

- [ ] **Step 1: 写下限测试**

`tests/test_order_recon.py` 追加:

```python
def test_buy_below_floor_is_dropped():
    slices = [_slice("usdt", cash=8.0)]
    # anchor 0.9990 -> rebuy 0.9989 < floor 0.9990 -> emit nothing
    out = desired_orders(0.9990, slices, rungs=[5], rebuy_off_bp=-1, tick=TICK, lot=LOT,
                         avail_base=0.0, avail_quote=8.0, min_qty=LOT, min_cost=1.0,
                         rebuy_floor_px=0.9990)
    assert out == {}


def test_buy_at_or_above_floor_kept():
    slices = [_slice("usdt", cash=8.0)]
    out = desired_orders(1.0000, slices, rungs=[5], rebuy_off_bp=-1, tick=TICK, lot=LOT,
                         avail_base=0.0, avail_quote=8.0, min_qty=LOT, min_cost=1.0,
                         rebuy_floor_px=0.9990)
    assert out[0].price == pytest.approx(0.9999)
```

- [ ] **Step 2: 跑测试验证失败**

Run: `PYTHONPATH=src python -m pytest tests/test_order_recon.py::test_buy_below_floor_is_dropped -q`
Expected: FAIL(`unexpected keyword 'rebuy_floor_px'`)

- [ ] **Step 3: 加参数 + skip 逻辑**

`desired_orders` 签名加 `rebuy_floor_px: float = 0.0,`(在 `min_sell_margin_bp` 后)。买单分支 `px = quantize_price("buy", raw, tick)` 后、`if px <= 0` 处改为:

```python
            px = quantize_price("buy", raw, tick)       # FLOOR -> never cross up
            if px <= 0 or px < rebuy_floor_px:          # non-positive OR below buy floor
                continue
```

- [ ] **Step 4: 跑测试验证通过**

Run: `PYTHONPATH=src python -m pytest tests/test_order_recon.py::test_buy_below_floor_is_dropped tests/test_order_recon.py::test_buy_at_or_above_floor_kept -q`
Expected: PASS

- [ ] **Step 5: engine reconcile 传参**

`src/sca/live/engine.py` reconcile 里 `desired_orders(...)` 调用加 `rebuy_floor_px=self.rebuy_floor_px,`。

- [ ] **Step 6: 全量测试 + commit**

Run: `PYTHONPATH=src python -m pytest tests/ -q` → PASS(默认 0.0 → 旧行为不变)

```bash
git add src/sca/live/order_recon.py src/sca/live/engine.py tests/test_order_recon.py
git commit -m "feat(live): 买侧下限 rebuy_floor_px,算出价<下限则不挂"
```

---

## Task 5: 去掉买侧 3bp band(reconcile 买单用 1-tick tol)

**Files:**
- Modify: `src/sca/live/engine.py:1764-1770`(去 spread/buy_tol,diff 不传 buy_price_tol)
- Test: `tests/test_order_recon.py`

- [ ] **Step 1: 写"无 band → 1tick 触发改单"测试**

`tests/test_order_recon.py` 追加(验证 diff_orders 不传 buy_price_tol 时买单用默认 tick tol):

```python
def test_buy_1tick_move_reprices_without_band():
    # no buy_price_tol -> buy uses default price_tol (1 tick) -> 1-tick move reprices
    desired = {0: Desired("buy", 0.9998, 5.0)}
    matched = {0: _live(0, Desired("buy", 0.9999, 5.0))}
    actions = diff_orders(desired, matched, price_tol=TICK, qty_tol=qty_tol_for(LOT))
    assert [a.kind for a in actions] == ["cancel", "place"]
```

- [ ] **Step 2: 跑测试验证(diff_orders 已支持默认 → 应直接 PASS)**

Run: `PYTHONPATH=src python -m pytest tests/test_order_recon.py::test_buy_1tick_move_reprices_without_band -q`
Expected: PASS(此测试锁定"去 band 后"的目标行为;diff_orders 默认 `buy_price_tol=None` 本已如此)

- [ ] **Step 3: 改 engine reconcile 去掉 buy_tol**

`src/sca/live/engine.py` 行 1764-1770 替换为(删 spread/buy_tol,diff 不传 buy_price_tol):

```python
        for a in diff_orders(desired, matched, meta["tick"], meta["lot"] / 2):
```

`buy_reprice_band` 函数保留(不删,避免动 import),仅不再被调用。

- [ ] **Step 4: 全量测试验证绿(engine 回归)**

Run: `PYTHONPATH=src python -m pytest tests/ -q`
Expected: PASS。若有 engine 测试断言旧 band 行为(3bp leave),按新语义(1tick 触发)更新它们并在 commit 说明。

- [ ] **Step 5: Commit**

```bash
git add src/sca/live/engine.py tests/test_order_recon.py
git commit -m "refactor(live): 去买侧 3bp band,买单改价改 1-tick 触发(hold 由 24h 冷却接管)"
```

---

## Task 6: 24h 冷却(挂满前保持现价,钉死不改)

**Files:**
- Modify: `src/sca/live/order_recon.py`(`desired_orders` 加 `now`/`min_hold_sec` + 冷却)
- Modify: `src/sca/live/engine.py`(reconcile 传 `now=now, min_hold_sec=self.rebuy_min_hold_sec`)
- Test: `tests/test_order_recon.py`

- [ ] **Step 1: 写冷却测试(未满 hold 保持现价 / 满 hold 改)**

`tests/test_order_recon.py` 追加:

```python
def test_buy_holds_price_within_min_hold():
    # live buy resting at 0.9999; fresh target would be 0.9998; aged 1h < 24h -> KEEP 0.9999
    s = _slice("usdt", cash=8.0, order_px=0.9999, order_side="buy", last_place_ts=1000.0)
    out = desired_orders(1.0000, [s], rungs=[5], rebuy_off_bp=-1, tick=TICK, lot=LOT,
                         avail_base=0.0, avail_quote=8.0, min_qty=LOT, min_cost=1.0,
                         ask=0.9999, now=1000.0 + 3600, min_hold_sec=86400)
    assert out[0].price == pytest.approx(0.9999)


def test_buy_reprices_after_min_hold():
    # same but aged 25h > 24h -> new target 0.9998
    s = _slice("usdt", cash=8.0, order_px=0.9999, order_side="buy", last_place_ts=1000.0)
    out = desired_orders(1.0000, [s], rungs=[5], rebuy_off_bp=-1, tick=TICK, lot=LOT,
                         avail_base=0.0, avail_quote=8.0, min_qty=LOT, min_cost=1.0,
                         ask=0.9999, now=1000.0 + 90000, min_hold_sec=86400)
    assert out[0].price == pytest.approx(0.9998)


def test_buy_no_now_skips_cooldown():
    # backtest path: now=None -> no cooldown, target computed fresh
    s = _slice("usdt", cash=8.0, order_px=0.9999, order_side="buy", last_place_ts=1000.0)
    out = desired_orders(1.0000, [s], rungs=[5], rebuy_off_bp=-1, tick=TICK, lot=LOT,
                         avail_base=0.0, avail_quote=8.0, min_qty=LOT, min_cost=1.0,
                         ask=0.9999, now=None, min_hold_sec=86400)
    assert out[0].price == pytest.approx(0.9998)
```

- [ ] **Step 2: 跑测试验证失败**

Run: `PYTHONPATH=src python -m pytest tests/test_order_recon.py::test_buy_holds_price_within_min_hold -q`
Expected: FAIL(`unexpected keyword 'now'`)

- [ ] **Step 3: 加冷却逻辑**

`desired_orders` 签名加 `now: float | None = None, min_hold_sec: float = 0.0,`。买单分支改为 **floor 检查(用 fresh target)在前、冷却 pin 在后**(Codex #2:脱锚撤出必须优先于 hold,否则 hold 把 px 覆盖成旧高价会架空 floor、继续接刀)。整段替换为:

```python
            px = quantize_price("buy", raw, tick)       # FRESH target
            if px <= 0 or px < rebuy_floor_px:          # floor on FRESH target: 脱锚 -> 撤出(优先于 hold)
                continue
            live_px = s.get("order_px")
            if (now is not None and live_px is not None            # have a resting buy
                    and s.get("order_side") == "buy"
                    and (now - (s.get("last_place_ts") or 0.0)) < min_hold_sec):
                px = float(live_px)                     # 24h hold: pin resting price (already floor-valid)
```

- [ ] **Step 4: 跑测试验证通过**

Run: `PYTHONPATH=src python -m pytest tests/test_order_recon.py -k "min_hold or cooldown" -q`
Expected: PASS(全 3 条)

- [ ] **Step 5: engine reconcile 传 now/min_hold_sec**

`src/sca/live/engine.py` reconcile `desired_orders(...)` 调用加 `now=now, min_hold_sec=self.rebuy_min_hold_sec,`。

- [ ] **Step 6: 全量测试 + commit**

Run: `PYTHONPATH=src python -m pytest tests/ -q` → PASS(now 默认 None → 回测/旧测试不变)

```bash
git add src/sca/live/order_recon.py src/sca/live/engine.py tests/test_order_recon.py
git commit -m "feat(live): 买单 24h 冷却,挂满前保持现价钉死不改"
```

---

## Task 7: 全量回归 + backtest 一致性 + dashboard 一致性

**Files:** 无改动(验证 task)

- [ ] **Step 1: 全量测试绿**

Run: `PYTHONPATH=src python -m pytest tests/ -q`
Expected: PASS(全绿)

- [ ] **Step 2: backtest headline 数字未变**

Run: `PYTHONPATH=src python -m pytest tests/ -k "backtest" -q`
Expected: PASS。回测走 `ask=None`/`now=None` fallback → `rebuy_price_raw` 退化为 `anchor−1bp`,headline APR 不变。若有金标数字测试(pin),确认 pin 不变。

- [ ] **Step 3: dashboard hold 期显示实际挂单价(Codex #5)**

`_status_rebuy_price`(engine:863)算的是 fresh target,但 hold 期实际挂单价是 pinned 的 `order_px` → dashboard 会误显 fresh target,重现"显示≠实际"的老困惑。改成:**有活买单时显示该 slice 的 `order_px`(真实挂单价),无活单时显示 fresh target**。

替换 `_status_rebuy_price`(863-869):

```python
    def _status_rebuy_price(self, anchor: float | None) -> float | None:
        if anchor is None:
            return None
        for s in self.slices:                       # hold-aware: 有活买单则显示真实挂单价 (Codex #5)
            if s.get("order_side") == "buy" and s.get("order_px") is not None:
                return float(s["order_px"])
        if self.mode in ("dryrun", "live"):
            tick = 10 ** -TICK_DP
            return quantize_price("buy", rebuy_price_raw(anchor, self.rebuy_off_bp, self.ask, tick), tick)
        return rounded_rebuy_price(anchor, self.rebuy_off_bp, TICK_DP)
```

测试(参考 `tests/test_engine*.py` 的 engine 构造 fixture;若无则最小构造一个 live-mode engine):

```python
def test_status_rebuy_shows_resting_price_when_held():
    eng = _make_live_engine()                       # 参考现有 test_engine fixture
    eng.slices[0].update(order_side="buy", order_px=1.0009)
    assert eng._status_rebuy_price(1.0011) == pytest.approx(1.0009)  # resting, not fresh target
```

Run: `PYTHONPATH=src python -m pytest tests/ -k "status_rebuy" -q` → PASS

- [ ] **Step 4: 最终 commit(若有 pin 更新)**

```bash
git add -A && git commit -m "test: 全量回归绿 + backtest/dashboard 一致性确认" --allow-empty
```

---

## Self-Review 结果

- **Spec 覆盖**:买价公式(T1)、ask 兜顶(T1)、下限(T4)、24h 冷却(T6)、去 band(T5)、last_place_ts 持久化(T3)、配置(T2)、回测 fallback(T1/T6/T7)、dashboard 同源(T7)、单测清单 11 条散落各 task ✓
- **无占位符**:每步含实际测试/实现代码 + 命令 + 预期 ✓
- **类型/签名一致**:`rebuy_price_raw(anchor, off, ask, tick)`、`desired_orders(..., ask, rebuy_floor_px, now, min_hold_sec)`、`_place(action, client, now)`、`last_place_ts` 全程一致 ✓
- **默认值回归保护**:ask=None/now=None/min_hold_sec=0/rebuy_floor_px=0 → 旧行为逐字节不变,回测不受影响 ✓
- **已知缺口**:engine 层 `_place` 记录 last_place_ts 的集成测试依赖现有 `tests/test_engine*.py` 的 fake-client pattern,实现者需参考(T3 Step 1 只锁字段默认值,记录逻辑靠 T7 全量回归 + 实现者补 fake-client 断言)。

---

## Codex 异构双审修复记录(2026-07-01, gpt-5.5 / effort high)

persona 三视角自审 + Codex 异构审查,共修 **3 P1 + 4 P2**:

- **P1 #1** config 默认值误开 USD1 → defaults 改 `0/0.0`,只在 USDC universe override 开(Task2 重构,守 USD1 零变化铁律)
- **P1 #2** floor 被 hold 覆盖架空(脱锚仍接刀)→ floor 用 fresh target、在冷却 pin 之前判(Task6 Step3)
- **P1 #3** last_place_ts crash 窗口(place 成功→persist 前崩→resume 立即改)→ 改 pre-call intent 一起持久化(Task3 Step3)
- **P2 #4** `_migrate_order_fields` 缺 last_place_ts 类型校验 → 加 `lambda x: x is None or isinstance(x,(int,float))`(Task3 Step3)
- **P2 #5** dashboard hold 期显示 fresh≠实际(重现"显示≠实际"老困惑)→ 有活买单显示真实 `order_px`(Task7 Step3)
- **P2 #6** `evaluate_fills`:763 若改 ask 会让模拟成交 `ask<=B` 永假、永不成交 → 保持 `bid` 不改(Task1 Step5);dryrun 走 reconcile 体现新逻辑、**仅 backtest 逐字节不变**
- **P2 #7** `rounded_rebuy_price` bid→ask kwarg 破坏现有 keyword caller → grep 全部调用点更新(Task1 Step5)

**结论:P0/P1 全修,无剩余阻塞项。** 待老板审批。
